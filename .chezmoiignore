i# ==========================================
# 1. 只要是服务器（不管是 Linux 还是别的），绝不需要 GUI 工具：
# ==========================================
{{ if eq .role "server" }}
.config/alacritty
.config/zellij
{{ end }}

# ==========================================
# 2. 只要是桌面端（不管是 Mac 还是 Ubuntu 桌面），不需要 Tmux（因为有 Zellij）：
# ==========================================
{{ if eq .role "desktop" }}
.tmux.conf
{{ end }}
